describe('DL.cycle()', function () {

    it('is define', function () {
        expect(DL.cycle).toBeDefined();
    });

    it('is function', function () {
        expect(DL.cycle).toEqual(jasmine.any(Function));
    });

    //TODO: [dmitry.makhnev] write tests
});